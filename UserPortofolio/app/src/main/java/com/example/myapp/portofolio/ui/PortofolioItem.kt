package com.example.myapp.portofolio.ui

/*
import androidx.compose.foundation.background
import androidx.compose.foundation.clickable
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Card
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import androidx.compose.ui.unit.dp
import com.example.myapp.portofolio.model.PortofolioData

@Composable
fun PortofolioItem(portofolioData: PortofolioData, onClick: (PortofolioData) -> Unit) {
    Card(
        modifier = Modifier
            .padding(8.dp)
            .fillMaxWidth()
            .clickable {
                // When the item is clicked, call the onClick function with the selected portofolioData
                onClick(portofolioData)
            }
            .background(Color.Gray), // Memberikan latar belakang warna abu-abu
    ) {
        Text(
            text = portofolioData.label,
            modifier = Modifier.padding(16.dp)
        )
    }
}
*/
